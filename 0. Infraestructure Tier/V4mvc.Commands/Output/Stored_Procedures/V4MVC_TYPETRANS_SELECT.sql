CREATE PROCEDURE [dbo].[V4MVC_TYPETRANS_SELECT]
AS
BEGIN

   SELECT
		IDTIPO,
		DSCTIPOV,
		DURACION,
		TIPOV,
		LICENCIAS,
		FECHAMOD,
		HORAMOD,
		IDSYNC
   FROM TYPETRANS
   ORDER BY DSCTIPOV ASC

END

